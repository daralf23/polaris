# New Branch

git checkout main
git pull origin main
git checkout -b feature/my-new-task

# Do Coding Things

git add .
git commit -m "feat: add user login button UI"
git push origin main


# Clean Up

git branch -d  /my-new-task
git push origin --delete feature/my-new-taskg 